JSON committee interface
========================

This is the same simulation as in the `thermodynamic_uq` example, but run
using a driver that returns ALL the committee members as entries in a JSON-formatted
string. 
